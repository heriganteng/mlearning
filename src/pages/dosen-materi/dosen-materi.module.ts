import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DosenMateriPage } from './dosen-materi';

@NgModule({
  declarations: [
    DosenMateriPage,
  ],
  imports: [
    IonicPageModule.forChild(DosenMateriPage),
  ],
})
export class DosenMateriPageModule {}
