import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DosenTokenPage } from './dosen-token';

@NgModule({
  declarations: [
    DosenTokenPage,
  ],
  imports: [
    IonicPageModule.forChild(DosenTokenPage),
  ],
})
export class DosenTokenPageModule {}
